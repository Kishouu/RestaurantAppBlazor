@page "/"

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-body text-center bg-light p-5 rounded-4">
                    <h1 class="display-5 fw-bold text-primary mb-4" tabindex="0">
                        🍽️ Welcome to the Restaurant App
                    </h1>
                    <p class="lead mb-5 text-secondary">
                        Manage dishes, handle orders, explore the gallery, and more.
                        Use the navigation menu to get started.
                    </p>
                    <a class="btn btn-lg btn-outline-primary me-3 px-4" href="/dishes" role="button" tabindex="0">
                        Browse Dishes
                    </a>
                    <a class="btn btn-lg btn-outline-secondary px-4" href="/orders" role="button" tabindex="0">
                        View Orders
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card-body {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .card-body:hover, .card-body:focus-within {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(27, 110, 194, 0.25);
        outline: none;
    }

    a.btn {
        text-decoration: none;              /* No underline */
        display: inline-block;
        padding: 0.75rem 1.5rem;            /* Comfortable padding */
        border-radius: 0.375rem;            /* Rounded corners */
        border: 2px solid transparent;
        font-weight: 600;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        cursor: pointer;
        transition: 
            background-color 0.25s ease, 
            color 0.25s ease, 
            box-shadow 0.25s ease, 
            transform 0.25s ease;
        will-change: transform, box-shadow;
        user-select: none;
        outline-offset: 3px;
    }

    a.btn:focus-visible {
        outline: 3px solid #1b6ec2;
        outline-offset: 3px;
    }

    /* Primary outline button */
    a.btn-outline-primary {
        color: #1b6ec2;
        border-color: #1b6ec2;
        background-color: transparent;
    }
    a.btn-outline-primary:hover,
    a.btn-outline-primary:focus {
        background-color: #1b6ec2;
        color: white;
        border-color: #1861ac;
        box-shadow: 0 6px 12px rgba(27, 110, 194, 0.5);
        transform: scale(1.05);
        text-decoration: none;
    }

    /* Secondary outline button */
    a.btn-outline-secondary {
        color: #6c757d;
        border-color: #6c757d;
        background-color: transparent;
    }
    a.btn-outline-secondary:hover,
    a.btn-outline-secondary:focus {
        background-color: #6c757d;
        color: white;
        border-color: #6c757d;
        box-shadow: 0 6px 12px rgba(108, 117, 125, 0.5);
        transform: scale(1.05);
        text-decoration: none;
    }
</style>

